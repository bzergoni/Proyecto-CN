extends layout
block content
        p.title Modificar aporte
        br
        br
        br
        
        if(!datosaporte)
          form(role='form', action="/modificarAporteRedir",method="post")
            .form-group
              input.form-control(type='text', name="dniRedir", placeholder='DNI')
              input.form-control(type='text', name="nombre_programaRedir", placeholder='nombre_programa')
            button(type='submit',class="boton") Aceptar
      
        if(datosaporte)
          if(!datosaporte.existe)
            form(role='form', action="/modificarAporteRedir",method="post")
              .form-group
                input.form-control(type='text', name="dniRedir", placeholder='DNI')
                input.form-control(type='text', name="nombre_programaRedir", placeholder='nombre_programa')
              button(type='submit',class="boton") Aceptar
            p el aporte que selecciono no existe actualmente  
        
          if(datosaporte.existe)
            form(role='form', action="/modificarAporte",method="post")
              .form-group
                label(for='dni') Dni del donante:
                input.form-control(type='text', name="dni", placeholder='DNI',value=datosaporte.dni,readonly="true")
                br
                label(for="nombre_programa") Nombre del programa:
                input.form-control(type='text', name="nombre_programa", placeholder='NOMBRE DEL PROGRAMA',value=datosaporte.nombre_programa,readonly="true")
                br
                label(for='monto') Monto del aporte:
                input.form-control(type='text', name="monto", placeholder='monto',value=datosaporte.monto)
                br
                label(for='frecuencia') Frecuencia con que aporta:
                select(name='frecuencia')
                  if (datosaporte.frecuencia == "Semestral")
                    option(value='Semestral',selected="true") Semestral
                  else
                    option(value='Semestral') Semestral
                  if (datosaporte.frecuencia == "Semestral")
                    option(value='Mensual',selected="true") Mensual
                  else
                    option(value='Mensual') Mensual
              
              button(type='submit',class="boton") Aceptar
